# MCP 任务拆分指南

对于任何需要修改多个文件或生成大量代码的任务，请按以下方式拆分：

- 每个子任务只涉及 1-2 个文件。
- 每个子任务只完成一个独立的功能点（例如：只修改产品详情页的样式，不碰其他文件）。
- 子任务之间通过 Git commit 隔离，确保每步可回滚。
- 每完成一个子任务，立即提交并推送，再启动下一个。
- 若在拆分后仍遇到超时，可以进一步使用"单步执行"模式：即一次只处理一个文件的修改，完成后由我确认再继续。
